# Assign missing fleets based on geographic coordinates.

Assign missing fleets based on geographic coordinates.

## Usage

``` r
fs_fleet_by_coordinates(df, polygon)
```

## Arguments

- df:

  A data frame

- polygon:

  A shapefile (class: SpatVector, geometry:polygons) with column "fleet"
